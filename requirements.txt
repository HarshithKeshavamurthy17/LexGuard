# Core web framework
streamlit>=1.30.0
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.7

# Data validation and configuration
pydantic>=2.6.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0

# LLM (choose one - Gemini recommended for free tier)
google-generativeai>=0.8.3
# openai>=1.3.0  # Uncomment if using OpenAI instead

# Embeddings - Use sentence-transformers for local/free embeddings (Ollama-style)
# This is required for Railway when using Ollama LLM (no API quotas)
sentence-transformers>=2.2.2  # Local embeddings - works with Ollama LLM

# Vector database (lightweight)
chromadb>=0.4.22

# Data processing (minimal versions)
pandas>=2.0.0
numpy>=1.24.0,<2.0.0  # Avoid numpy 2.x for compatibility

# PDF and document processing
pypdf>=4.0.0
python-docx>=1.1.0
pillow>=10.0.0
# pdf2image>=1.17.0  # Optional - only if OCR needed
# pytesseract>=0.3.10  # Optional - only if OCR needed

# Report generation
reportlab>=4.0.0

# Visualization
plotly>=5.18.0

# HTTP client
httpx>=0.26.0

# Remove heavy dependencies
# langchain>=0.1.0  # Not needed - we use direct API calls
# langchain-community>=0.0.10
# langchain-openai>=0.0.5
# langchain-google-genai>=0.0.5
# watchdog>=4.0.0  # Development only
